ASSUME CS:CODE,DS:DATA1
DATA1 SEGMENT
BIG DW ?
SMALL DW ?
;PNTPC DATA1
DATA1 ENDS
CODE SEGMENT
START:  MOV AX,DATA1
        MOV DS,AX
        LEA SI,DATA1
        MOV BL,[SI]
        INC SI
        CMP BL,[SI]
        JG NEXT
        MOV BIG,[SI]
        MOV SMALL,BL
NEXT:   MOV BIG,BL
        MOV SMALL,[SI]
DONE:   MOV AX,4C00H
        INT 21H
CODE ENDS
END START
